program: Train.py
project: sweep-full
entity: mahee-velu-iitm   # (optional) uncomment to pin the entity

method: bayes
metric:
  name: val_acc
  goal: maximize

parameters:
  activation:
    values: ["relu", "silu", "gelu", "tanh", "sigmoid"]
  optimizer:
    values: ["sgd", "sgd_nesterov", "adam", "adagrad", "rmsprop", "nadam"]
  batch_size:
    values: [64, 128, 256]
  epochs:
    values: [10, 50, 100]

  # Use log_uniform_values when specifying real limits (not ln-exponents)
  lr:
    distribution: log_uniform_values
    min: 0.0005
    max: 0.2

  weight_decay:
    distribution: log_uniform_values
    min: 1e-5
    max: 1e-3

  scheduler:
    values: ["cosine"]
  autoaugment:
    values: [false, true]
  seed:
    values: [42]
